rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only access their own progress data
    match /users/{userId}/progress/{puzzleId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
